# Debug builds use nonempty process values first, then ./.env. Copy this file
# to .env and fill in only the tokens needed for local testing. Never commit it.
AIRBORNE_GITHUB_TOKEN=
AIRBORNE_BUILDKITE_TOKEN=
